import SwiftUI
struct LibraryView: View { var body: some View { ScrollView { VStack(alignment:.leading,spacing:18){Text("المكتبة").font(.largeTitle.bold());Text("كل لوحاتك في مكان واحد").foregroundStyle(.white.opacity(0.55));ForEach(["الخلايا والطاقة","تاريخ الدولة السعودية","أساسيات الشبكات","المحاسبة المالية"],id:\.self){t in HStack{RoundedRectangle(cornerRadius:16).fill(.thinMaterial).frame(width:58,height:58).overlay(Image(systemName:"square.grid.2x2.fill"));VStack(alignment:.leading){Text(t).font(.headline);Text("لوحة ذكية • محفوظة").font(.caption).foregroundStyle(.white.opacity(0.45))};Spacer();Image(systemName:"ellipsis")} .padding(12).background(Color.white.opacity(0.045),in:RoundedRectangle(cornerRadius:20))}}.padding(20).padding(.bottom,120)}} }
