# GeoShift

Independent iOS location-service testing application with SwiftUI + MapKit and an embedded Go core.

## Build

On macOS with Xcode installed:

```bash
./build.sh
```

Output:

```text
dist/GeoShift-unsigned.ipa
```

GitHub Actions also builds the unsigned IPA automatically and publishes it as a workflow artifact.
